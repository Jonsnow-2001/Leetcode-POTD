
class Solution {
public:
    int minBitFlips(int start, int goal) {
        int ans=start^goal, cnt=0;

        for(int i=0;i<32;i++){
            int bit=(1<<i)&ans;
            if(bit) cnt++;
        }

        return cnt;
    }
};
